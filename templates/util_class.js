/**
 * @module point
 */

/**
 * Creates a new Point object.
 * @class Point
 * @param {number} x - The x
 * @property {number} x - The x
 * @see module:point
 */
var Point = function ( x, y )
{
    this.x = x;
    this.y = y;
};

/**
* Calculates the slope to another point.
* @memberof! module:point~Point#
* @function slopeToOtherPoint
* @param {number} arg - An argument
* @returns {number} A value
*/
Point.prototype.theFuction = function ( arg )
{

    // logic
};

module.exports = Point;